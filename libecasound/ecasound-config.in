#!/bin/sh

usage()
{
	echo "usage: $0 [OPTIONS]"
cat << EOH

options:
	[--libs]
	[--debuglibs]
	[--cflags]
	[--version]
EOH
	exit 1;
}

prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@

flags=""

while test $# -gt 0
do
  case $1 in
    --libs)
	  flags="$flags -L$exec_prefix/lib/ecasound -lecasound -lkvutils -Wl,--rpath -Wl,$exec_prefix/lib/ecasound"
	  ;;
    --debuglibs)
	  flags="$flags -L$exec_prefix/lib/ecasound -lecasound -lkvutils -Wl,--rpath -Wl,$exec_prefix/lib/ecasound"
	  ;;
    --cflags)
	  flags="$flags -I$includedir/ecasound"
	  ;;
    --version)
	  echo @VERSION@
	  ;;
	*)
	  echo "$0: unknown option $1"
	  echo 
	  usage
	  ;;
  esac
  shift
done

if test -n "$flags"
then
  echo $flags
fi
